        pipeline {
        agent none
        tools {
          maven 'maven'
        }
        stages {
          stage("build & SonarQube analysis") {
            agent any
            steps {
              withSonarQubeEnv('sonar') {
                sh 'mvn clean package sonar:sonar -Dsonar.host.url=http://3.15.178.106:9000/ -Dsonar.login=admin -Dsonar.password=admin -Dsonar.inclusions=**/*.java -Dsonar.exclusions=src/test/java/servlet/*.java
              }
            }
          }
          stage("Quality Gate") {
            steps {
              timeout(time: 1, unit: 'HOURS') {
                waitForQualityGate abortPipeline: true     
              }
            }
          }
        }
      }

